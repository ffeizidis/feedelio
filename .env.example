# No login for local Docker. Keep the published port bound to 127.0.0.1.
FEEDELIO_NO_AUTH=1
# Optional remote authentication: set NO_AUTH=0 and a strong token.
FEEDELIO_TOKEN=
# Opt in only if you subscribe to feeds or use a proxy on a private network.
FEEDELIO_ALLOW_PRIVATE_NETWORK=0
